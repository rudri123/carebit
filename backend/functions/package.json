{
  "name": "carebit-functions",
  "private": true,
  "version": "1.0.0",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "npm run build && node --test test/fitbit_callback_flow.test.js test/firestore_runtime.test.js test/start_emulators.test.js",
    "preflight:emulators": "node scripts/start-emulators.cjs --preflight-only",
    "serve": "npm run build && node scripts/start-emulators.cjs",
    "serve:functions-only": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.1"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "typescript": "^5.9.2"
  }
}
